run_id: R001
seed: 1337
split_to_inject: test

fault_types:
  bias: true
  drift: true
  lag: true
  stuck_at: true
  dropout: true
  noise_increase: true   # robustness-only

magnitude_tiers:
  small: 1.0
  medium: 2.5
  large: 5.0

duration_tiers:
  short: 10
  medium: 30
  long: 60

min_separation_samples: 20

events_per_channel: 2

allowed_channels:
  - TEMP_BAT
  - TEMP_CPU
  - TEMP_PANEL_X
  - TEMP_PANEL_Y
  - TEMP_PAYLOAD
  - TEMP_STRUCTURE

schema_version: schema_v0_1